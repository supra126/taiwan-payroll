{
  "name": "taiwan-payroll-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "validate:data": "tsx scripts/validate-data.ts",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
